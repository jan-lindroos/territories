[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.13"]
build-backend = "scikit_build_core.build"

[project]
name = "territories"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pytest"]

[project.scripts]
territories = "cli:main"

[tool.scikit-build.wheel]
packages = []
